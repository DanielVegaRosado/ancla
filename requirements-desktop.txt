# Extra dependencies to package and run the desktop version (native window
# instead of a browser tab). Not needed to use the app from source with
# `python run.py`: that path only needs requirements.txt.
-r requirements.txt
pywebview>=5.0
pyinstaller>=6.0
