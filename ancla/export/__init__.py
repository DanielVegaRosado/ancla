"""Turns a proposal into a laid-out CV page.

`html_layout.py` + `html_templates.py` lay the content out as a page the
user's browser prints to PDF — the only export path; see
`html-templates/README.md` for the template contract. `sidecar.py` holds
the `.yaml` sidecar reader shared by template-discovery modules.
"""
