# The extra Python-only dependency the Flatpak build needs on top of
# requirements.txt (build-desktop.yml concatenates the two before handing
# them to flatpak-pip-generator -- a nested `-r requirements.txt` here
# doesn't work, the generator resolves it against its own temporary copy
# of this file, not against the repo). Unlike requirements-desktop.txt
# (Windows/macOS, PyInstaller-based), this path never needs pyinstaller:
# org.gnome.Sdk/Platform already supplies Python, GTK3, WebKit2GTK and
# PyGObject.
pywebview>=5.0
